# Dependencies for personal_ebird_explorer notebook (Binder + pip install -r)
jupyter
jupyterlab
voila
# Pandas 3 is not available on all Binder/Python combos; 2.x matches Streamlit and installs everywhere.
pandas>=2.0,<3
folium
ipywidgets>=8
whoosh
scikit-learn
pycountry

# Testing
pytest
