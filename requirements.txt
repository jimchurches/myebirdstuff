#
# Single dependency set for:
# - core library + tests
# - Streamlit app runtime
#
# Note: Streamlit currently requires pandas<3, so this repo targets pandas 2.x.
pandas>=2.0,<3

# Core deps
folium>=0.15
whoosh
scikit-learn>=1.3,<2
pycountry>=23.12.11
PyYAML>=6,<7

# Streamlit app deps
streamlit>=1.40
watchdog>=4,<7  # efficient file watching on reload (Streamlit falls back to polling without it)
streamlit-folium>=0.20.0
streamlit-searchbox>=0.1.24
pydantic>=2,<3

# Testing
pytest
