# Base dependencies (non-Streamlit).
#
# For the Streamlit app, use `requirements-streamlit.txt` or `streamlit_app/requirements.txt`
# (those include Streamlit-specific dependencies like streamlit-folium).
pandas>=3.0,<4
folium
whoosh
scikit-learn
pycountry

# Testing
pytest
PyYAML>=6,<7
