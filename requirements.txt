#
# Single dependency set for:
# - core library + tests
# - Streamlit app runtime
#
# Note: Streamlit currently requires pandas<3, so this repo targets pandas 2.x.
pandas>=3.0.5,<4

# Core deps
whoosh
scikit-learn>=1.9.0,<2
pycountry>=26.2.16
PyYAML>=6.0.3,<7
# Google Geocoding for Maintenance → Create location name from GPS (also used by scripts/eBirdChecklistNameFromGPS.py)
requests>=2.34.2,<3

# Streamlit app deps
streamlit>=1.60.0
watchdog>=6.0.0,<7  # efficient file watching on reload (Streamlit falls back to polling without it)
streamlit-searchbox>=0.1.24
pydantic>=2.13.4,<3

# Share-summary PNG export (headless Chromium screenshot)
playwright>=1.62.0,<2

# Testing
pytest
pytest-cov>=7.1.0,<8
# Transitive (e.g. rich, pytest); Pygments <2.20 affected by CVE-2026-4539 (ReDoS).
Pygments>=2.20.0

# Linting (CI + local; ruff checks explorer/ — see ruff.toml)
ruff>=0.16.1,<1
