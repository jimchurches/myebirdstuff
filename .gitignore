.DS_Store
# Python virtual environments (any name)
.venv/
.venv-*/
venv/
env/

# Don't track secrets or local config paths
scripts/config.py
scripts/config_secret.py
config_secret.py
config/config.yaml
config/config_secret.yaml
.streamlit/secrets.toml
__pycache__/

# Ruff (local cache + ad-hoc pip --target installs, e.g. agent/sandbox tooling)
.ruff_cache/
_ruff_local/

# pytest-cov / coverage.py
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.hypothesis/

# Local Explorer perf snapshots (machine-specific JSON; archive via scripts/snapshot_explorer_perf_log.py)
benchmarks/map_perf/snapshots/

# Local-only real eBird export used for #205 perf investigation (never committed)
# Scoped to MyEBirdData*.csv so the tracked tests/fixtures/ebird_integration_fixture.csv stays committed.
MyEBirdData*.csv
tests/fixtures/MyEBirdData*.csv