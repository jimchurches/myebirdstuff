[pytest]
pythonpath = .
addopts = --import-mode=importlib
markers =
    e2e: browser end-to-end tests (optional/manual by default)
    perf: opt-in Explorer map perf + logged JSON timings (needs --perf; see docs/development.md)
